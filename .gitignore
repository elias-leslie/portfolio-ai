# Python
__pycache__/
*.py[cod]
*.so
.Python
.venv/
venv/

# Pip artifacts (from pip install package>=version)
=*

# Node / Next.js
node_modules/
.npm/
.pnpm-store/
web/.next/
web/out/
web/.turbo/

# Project data / logs
data/
logs/

# Celery runtime files (scheduler state database)
celerybeat-schedule
celerybeat-schedule.db
celerybeat-schedule-shm
celerybeat-schedule-wal

# Tool caches (should only exist in backend/)
/.mypy_cache/
/.ruff_cache/

# Prevent root-level duplicates (files belong in backend/)
/tests/

# Prevent nested backend directory (legacy artifact)
/backend/backend/

# Environment & tooling
.env
.env.local
.idea/
.vscode/
*.swp
*.swo
Thumbs.db
.DS_Store

# Ignore deb files
*.deb
*.rpm
*.tar.gz
*.tar.bz2
*.tar.xz
*.tar.lzma
*.tar.lz
*.tar.zst
*.tar.zstd
*.tar.zstd

# References
references/
app/references/

# API Keys
api_keys.md

# Claude (ignore settings but track commands)
.claude/settings.local.json
# Note: .claude/skills/browser-automation/node_modules/ already ignored by global node_modules/ rule

# Playwright artifacts (replaced with Chrome DevTools MCP)
.playwright-mcp/
playwright-report/
test-results/

# Command validation state
.validation-state.json
backend/.venv.python312.backup/
backups/
